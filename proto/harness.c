#include <stdlib.h>
#include <stdio.h>

#include "message.h"

int main() {

    // intitialize test vars
    MESSAGE_TYPE_T msg_type = PUBLIC_MESSAGE;

    user_t *sender_user = malloc(sizeof(user_t));
    sender_user->user_id = 1;
    sender_user->username = "testuser";
    
                

} 
